import SettingsSection from "@/container/dashboard-page/settings-section";
import React from "react";

const Settings = () => {
  return (
    <main className=" h-full">
      <SettingsSection />
    </main>
  );
};

export default Settings;
